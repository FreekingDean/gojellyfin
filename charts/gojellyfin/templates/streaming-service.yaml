{{- if .Values.streaming.enabled }}
{{- $component := "streaming" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gojellyfin.fullname" . }}-streaming
  labels:
    {{- include "gojellyfin.labels" (dict "root" $ "component" $component) | nindent 4 }}
  {{- with .Values.streaming.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.streaming.service.type }}
  selector:
    {{- include "gojellyfin.selectorLabels" (dict "root" $ "component" $component) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.streaming.service.port }}
      targetPort: http
{{- end }}
