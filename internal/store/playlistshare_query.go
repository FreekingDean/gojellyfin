



// Code generated by ent, DO NOT EDIT.



package store



	
import (
	"context"
	"errors"
	"fmt"
	"math"
	"strings"
	"sync"
	"time"
		"github.com/FreekingDean/gojellyfin/internal/store/predicate"
				"github.com/google/uuid"
			"github.com/google/uuid"
	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/schema/field"
			"database/sql/driver"
			"entgo.io/ent/dialect/sql"
			"entgo.io/ent/dialect/sql/sqlgraph"
			"entgo.io/ent/dialect/sql/sqljson"
			"entgo.io/ent/schema/field"
			 "github.com/FreekingDean/gojellyfin/internal/store/playlistshare"
			 "github.com/FreekingDean/gojellyfin/internal/store/playlist"
			 "github.com/FreekingDean/gojellyfin/internal/store/user"

)






// PlaylistShareQuery is the builder for querying PlaylistShare entities.
type PlaylistShareQuery struct {
	config
	ctx			*QueryContext
	order		[]playlistshare.OrderOption
	inters		[]Interceptor
	predicates 	[]predicate.PlaylistShare
		withPlaylist *PlaylistQuery
		withUser *UserQuery
		withFKs bool
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlaylistShareQuery builder.
func (_q *PlaylistShareQuery) Where(ps ...predicate.PlaylistShare) *PlaylistShareQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PlaylistShareQuery) Limit(limit int) *PlaylistShareQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PlaylistShareQuery) Offset(offset int) *PlaylistShareQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PlaylistShareQuery) Unique(unique bool) *PlaylistShareQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PlaylistShareQuery) Order(o ...playlistshare.OrderOption) *PlaylistShareQuery {
	_q.order = append(_q.order, o...)
	return _q
}



	
	// QueryPlaylist chains the current query on the "playlist" edge.
	func (_q *PlaylistShareQuery) QueryPlaylist() *PlaylistQuery {
		query := (&PlaylistClient{config: _q.config}).Query()
		query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
			if err := _q.prepareQuery(ctx); err != nil {
				return nil, err
			}  
	selector := _q.sqlQuery(ctx)
	if err := selector.Err(); err != nil {
		return nil, err
	}
	step := sqlgraph.NewStep(
		sqlgraph.From(playlistshare.Table, playlistshare.FieldID, selector),
		sqlgraph.To(playlist.Table, playlist.FieldID),
		sqlgraph.Edge(sqlgraph.M2O, true, playlistshare.PlaylistTable,playlistshare.PlaylistColumn),
	)
	fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
return fromU, nil
		}
		return query
	}

	
	// QueryUser chains the current query on the "user" edge.
	func (_q *PlaylistShareQuery) QueryUser() *UserQuery {
		query := (&UserClient{config: _q.config}).Query()
		query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
			if err := _q.prepareQuery(ctx); err != nil {
				return nil, err
			}  
	selector := _q.sqlQuery(ctx)
	if err := selector.Err(); err != nil {
		return nil, err
	}
	step := sqlgraph.NewStep(
		sqlgraph.From(playlistshare.Table, playlistshare.FieldID, selector),
		sqlgraph.To(user.Table, user.FieldID),
		sqlgraph.Edge(sqlgraph.M2O, true, playlistshare.UserTable,playlistshare.UserColumn),
	)
	fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
return fromU, nil
		}
		return query
	}


// First returns the first PlaylistShare entity from the query. 
// Returns a *NotFoundError when no PlaylistShare was found.
func (_q *PlaylistShareQuery) First(ctx context.Context) (*PlaylistShare, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ playlistshare.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PlaylistShareQuery) FirstX(ctx context.Context) *PlaylistShare {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}


	// FirstID returns the first PlaylistShare ID from the query.
	// Returns a *NotFoundError when no PlaylistShare ID was found.
	func (_q *PlaylistShareQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
		var ids []uuid.UUID
		if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
			return
		}
		if len(ids) == 0 {
			err = &NotFoundError{ playlistshare.Label}
			return
		}
		return ids[0], nil
	}

	// FirstIDX is like FirstID, but panics if an error occurs.
	func (_q *PlaylistShareQuery) FirstIDX(ctx context.Context) uuid.UUID {
		id, err := _q.FirstID(ctx)
		if err != nil && !IsNotFound(err) {
			panic(err)
		}
		return id
	}


// Only returns a single PlaylistShare entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlaylistShare entity is found.
// Returns a *NotFoundError when no PlaylistShare entities are found.
func (_q *PlaylistShareQuery) Only(ctx context.Context) (*PlaylistShare, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ playlistshare.Label}
	default:
		return nil, &NotSingularError{ playlistshare.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PlaylistShareQuery) OnlyX(ctx context.Context) *PlaylistShare {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}


	// OnlyID is like Only, but returns the only PlaylistShare ID in the query.
	// Returns a *NotSingularError when more than one PlaylistShare ID is found.
	// Returns a *NotFoundError when no entities are found.
	func (_q *PlaylistShareQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
		var ids []uuid.UUID
		if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
			return
		}
		switch len(ids) {
		case 1:
			id = ids[0]
		case 0:
			err = &NotFoundError{ playlistshare.Label}
		default:
			err = &NotSingularError{ playlistshare.Label}
		}
		return
	}

	// OnlyIDX is like OnlyID, but panics if an error occurs.
	func (_q *PlaylistShareQuery) OnlyIDX(ctx context.Context) uuid.UUID {
		id, err := _q.OnlyID(ctx)
		if err != nil {
			panic(err)
		}
		return id
	}


// All executes the query and returns a list of PlaylistShares.
func (_q *PlaylistShareQuery) All(ctx context.Context) ([]*PlaylistShare, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlaylistShare, *PlaylistShareQuery]()
	return withInterceptors[[]*PlaylistShare](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PlaylistShareQuery) AllX(ctx context.Context) []*PlaylistShare {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}


	// IDs executes the query and returns a list of PlaylistShare IDs.
	func (_q *PlaylistShareQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
		if _q.ctx.Unique == nil && _q.path != nil {
			_q.Unique(true)
		}
		ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
			if err = _q.Select(playlistshare.FieldID).Scan(ctx, &ids); err != nil {
				return nil, err
			}
		return ids, nil
	}

	// IDsX is like IDs, but panics if an error occurs.
	func (_q *PlaylistShareQuery) IDsX(ctx context.Context) []uuid.UUID {
		ids, err := _q.IDs(ctx)
		if err != nil {
			panic(err)
		}
		return ids
	}


// Count returns the count of the given query.
func (_q *PlaylistShareQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PlaylistShareQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PlaylistShareQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PlaylistShareQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx);{
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("store: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PlaylistShareQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlaylistShareQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PlaylistShareQuery) Clone() *PlaylistShareQuery {
	if _q == nil {
		return nil
	}
	return &PlaylistShareQuery{
		config: 	_q.config,
		ctx: 		_q.ctx.Clone(),
		order: 		append([]playlistshare.OrderOption{}, _q.order...),
		inters: 	append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.PlaylistShare{}, _q.predicates...),
			withPlaylist: _q.withPlaylist.Clone(),
			withUser: _q.withUser.Clone(),
		// clone intermediate query.
		sql: _q.sql.Clone(),
		path: _q.path,
	}
}
	
	
	// WithPlaylist tells the query-builder to eager-load the nodes that are connected to
	// the "playlist" edge. The optional arguments are used to configure the query builder of the edge.
	func (_q *PlaylistShareQuery) WithPlaylist(opts ...func(*PlaylistQuery)) *PlaylistShareQuery {
		query := (&PlaylistClient{config: _q.config}).Query()
		for _, opt := range opts {
			opt(query)
		}
		_q.withPlaylist = query
		return _q
	}
	
	
	// WithUser tells the query-builder to eager-load the nodes that are connected to
	// the "user" edge. The optional arguments are used to configure the query builder of the edge.
	func (_q *PlaylistShareQuery) WithUser(opts ...func(*UserQuery)) *PlaylistShareQuery {
		query := (&UserClient{config: _q.config}).Query()
		for _, opt := range opts {
			opt(query)
		}
		_q.withUser = query
		return _q
	}



// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlaylistShare.Query().
//		GroupBy(playlistshare.FieldCreatedAt).
//		Aggregate(store.Count()).
//		Scan(ctx, &v)
//
func (_q *PlaylistShareQuery) GroupBy(field string, fields ...string) *PlaylistShareGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlaylistShareGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = playlistshare.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}



// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PlaylistShare.Query().
//		Select(playlistshare.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (_q *PlaylistShareQuery) Select(fields ...string) *PlaylistShareSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PlaylistShareSelect{ PlaylistShareQuery: _q }
	sbuild.label = playlistshare.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlaylistShareSelect configured with the given aggregations.
func (_q *PlaylistShareQuery) Aggregate(fns ...AggregateFunc) *PlaylistShareSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PlaylistShareQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("store: uninitialized interceptor (forgotten import store/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !playlistshare.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("store: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}


	
	




func (_q *PlaylistShareQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlaylistShare, error) {
	var (
		nodes = []*PlaylistShare{}
			withFKs = _q.withFKs
		_spec = _q.querySpec()
			loadedTypes = [2]bool{
					_q.withPlaylist != nil,
					_q.withUser != nil,
			}
	)
			if _q.withPlaylist != nil || _q.withUser != nil {
				withFKs = true
			}
		if withFKs {
			_spec.Node.Columns = append(_spec.Node.Columns, playlistshare.ForeignKeys...)
		}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlaylistShare).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlaylistShare{config: _q.config}
		nodes = append(nodes, node)
			node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
		if query := _q.withPlaylist; query != nil {
			if err := _q.loadPlaylist(ctx, query, nodes, nil,
					func(n *PlaylistShare, e *Playlist){ n.Edges.Playlist = e }); err != nil {
				return nil, err
			}
		}
		if query := _q.withUser; query != nil {
			if err := _q.loadUser(ctx, query, nodes, nil,
					func(n *PlaylistShare, e *User){ n.Edges.User = e }); err != nil {
				return nil, err
			}
		}
	return nodes, nil
}


	func (_q *PlaylistShareQuery) loadPlaylist(ctx context.Context, query *PlaylistQuery, nodes []*PlaylistShare, init func(*PlaylistShare), assign func(*PlaylistShare, *Playlist)) error {
			ids := make([]uuid.UUID, 0, len(nodes))
			nodeids := make(map[uuid.UUID][]*PlaylistShare)
			for i := range nodes {
					if nodes[i].playlist_shares == nil {
						continue
					}
				fk := *nodes[i].playlist_shares
				if _, ok := nodeids[fk]; !ok {
					ids = append(ids, fk)
				}
				nodeids[fk] = append(nodeids[fk], nodes[i])
			}
			if len(ids) == 0 {
				return nil
			}
			query.Where(playlist.IDIn(ids...))
			neighbors, err := query.All(ctx)
			if err != nil {
				return err
			}
			for _, n := range neighbors {
				nodes, ok := nodeids[n.ID]
				if !ok {
					return fmt.Errorf(`unexpected foreign-key "playlist_shares" returned %v`, n.ID)
				}
				for i := range nodes {
					assign(nodes[i], n)
				}
			}
		return nil
	}
	func (_q *PlaylistShareQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*PlaylistShare, init func(*PlaylistShare), assign func(*PlaylistShare, *User)) error {
			ids := make([]uuid.UUID, 0, len(nodes))
			nodeids := make(map[uuid.UUID][]*PlaylistShare)
			for i := range nodes {
					if nodes[i].user_playlist_shares == nil {
						continue
					}
				fk := *nodes[i].user_playlist_shares
				if _, ok := nodeids[fk]; !ok {
					ids = append(ids, fk)
				}
				nodeids[fk] = append(nodeids[fk], nodes[i])
			}
			if len(ids) == 0 {
				return nil
			}
			query.Where(user.IDIn(ids...))
			neighbors, err := query.All(ctx)
			if err != nil {
				return err
			}
			for _, n := range neighbors {
				nodes, ok := nodeids[n.ID]
				if !ok {
					return fmt.Errorf(`unexpected foreign-key "user_playlist_shares" returned %v`, n.ID)
				}
				for i := range nodes {
					assign(nodes[i], n)
				}
			}
		return nil
	}

func (_q *PlaylistShareQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
		_spec.Node.Columns = _q.ctx.Fields
		if len(_q.ctx.Fields) > 0 {
			_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
		}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PlaylistShareQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(playlistshare.Table, playlistshare.Columns, sqlgraph.NewFieldSpec(playlistshare.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
			_spec.Node.Columns = append(_spec.Node.Columns, playlistshare.FieldID)
			for i := range fields {
				if fields[i] != playlistshare.FieldID {
					_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
				}
			}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}





func (_q *PlaylistShareQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(playlistshare.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = playlistshare.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

    

    











// PlaylistShareGroupBy is the group-by builder for PlaylistShare entities.
type PlaylistShareGroupBy struct {
	selector
	build *PlaylistShareQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PlaylistShareGroupBy) Aggregate(fns ...AggregateFunc) *PlaylistShareGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PlaylistShareGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlaylistShareQuery, *PlaylistShareGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}


	
	



func (_g *PlaylistShareGroupBy) sqlScan(ctx context.Context, root *PlaylistShareQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds) + len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}







// PlaylistShareSelect is the builder for selecting fields of PlaylistShare entities.
type PlaylistShareSelect struct {
	*PlaylistShareQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PlaylistShareSelect) Aggregate(fns ...AggregateFunc) *PlaylistShareSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PlaylistShareSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlaylistShareQuery, *PlaylistShareSelect](ctx, _s.PlaylistShareQuery, _s, _s.inters, v)
}


	
	



func (_s *PlaylistShareSelect) sqlScan(ctx context.Context, root *PlaylistShareQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}



    






