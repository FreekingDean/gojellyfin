// Code generated by ent, DO NOT EDIT.

package store

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/FreekingDean/gojellyfin/internal/store/syncplaygroup"
	"github.com/google/uuid"
)

// SyncPlayGroup is the model entity for the SyncPlayGroup schema.
type SyncPlayGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// State holds the value of the "state" field.
	State syncplaygroup.State `json:"state,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SyncPlayGroupQuery when eager-loading is set.
	Edges        SyncPlayGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SyncPlayGroupEdges holds the relations/edges for other nodes in the graph.
type SyncPlayGroupEdges struct {
	// Members holds the value of the members edge.
	Members []*SyncPlayGroupMember `json:"members,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MembersOrErr returns the Members value or an error if the edge
// was not loaded in eager-loading.
func (e SyncPlayGroupEdges) MembersOrErr() ([]*SyncPlayGroupMember, error) {
	if e.loadedTypes[0] {
		return e.Members, nil
	}
	return nil, &NotLoadedError{edge: "members"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncPlayGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case syncplaygroup.FieldName, syncplaygroup.FieldState:
			values[i] = new(sql.NullString)
		case syncplaygroup.FieldCreatedAt, syncplaygroup.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case syncplaygroup.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncPlayGroup fields.
func (_m *SyncPlayGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case syncplaygroup.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case syncplaygroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case syncplaygroup.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case syncplaygroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case syncplaygroup.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = syncplaygroup.State(value.String)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncPlayGroup.
// This includes values selected through modifiers, order, etc.
func (_m *SyncPlayGroup) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryMembers queries the "members" edge of the SyncPlayGroup entity.
func (_m *SyncPlayGroup) QueryMembers() *SyncPlayGroupMemberQuery {
	return NewSyncPlayGroupClient(_m.config).QueryMembers(_m)
}

// Update returns a builder for updating this SyncPlayGroup.
// Note that you need to call SyncPlayGroup.Unwrap() before calling this method if this SyncPlayGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SyncPlayGroup) Update() *SyncPlayGroupUpdateOne {
	return NewSyncPlayGroupClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SyncPlayGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SyncPlayGroup) Unwrap() *SyncPlayGroup {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("store: SyncPlayGroup is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SyncPlayGroup) String() string {
	var builder strings.Builder
	builder.WriteString("SyncPlayGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("state=")
	builder.WriteString(fmt.Sprintf("%v", _m.State))
	builder.WriteByte(')')
	return builder.String()
}

// SyncPlayGroups is a parsable slice of SyncPlayGroup.
type SyncPlayGroups []*SyncPlayGroup
