// Code generated by ent, DO NOT EDIT.

package store

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/FreekingDean/gojellyfin/internal/store/credit"
	"github.com/FreekingDean/gojellyfin/internal/store/item"
	"github.com/FreekingDean/gojellyfin/internal/store/person"
	"github.com/google/uuid"
)

// Credit is the model entity for the Credit schema.
type Credit struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind credit.Kind `json:"kind,omitempty"`
	// Role holds the value of the "role" field.
	Role string `json:"role,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int32 `json:"sort_order,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CreditQuery when eager-loading is set.
	Edges          CreditEdges `json:"edges"`
	item_credits   *uuid.UUID
	person_credits *uuid.UUID
	selectValues   sql.SelectValues
}

// CreditEdges holds the relations/edges for other nodes in the graph.
type CreditEdges struct {
	// Item holds the value of the item edge.
	Item *Item `json:"item,omitempty"`
	// Person holds the value of the person edge.
	Person *Person `json:"person,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ItemOrErr returns the Item value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CreditEdges) ItemOrErr() (*Item, error) {
	if e.Item != nil {
		return e.Item, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: item.Label}
	}
	return nil, &NotLoadedError{edge: "item"}
}

// PersonOrErr returns the Person value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CreditEdges) PersonOrErr() (*Person, error) {
	if e.Person != nil {
		return e.Person, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: person.Label}
	}
	return nil, &NotLoadedError{edge: "person"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Credit) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case credit.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case credit.FieldKind, credit.FieldRole:
			values[i] = new(sql.NullString)
		case credit.FieldCreatedAt, credit.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case credit.FieldID:
			values[i] = new(uuid.UUID)
		case credit.ForeignKeys[0]: // item_credits
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case credit.ForeignKeys[1]: // person_credits
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Credit fields.
func (_m *Credit) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case credit.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case credit.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case credit.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case credit.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = credit.Kind(value.String)
			}
		case credit.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = value.String
			}
		case credit.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				_m.SortOrder = int32(value.Int64)
			}
		case credit.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field item_credits", values[i])
			} else if value.Valid {
				_m.item_credits = new(uuid.UUID)
				*_m.item_credits = *value.S.(*uuid.UUID)
			}
		case credit.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field person_credits", values[i])
			} else if value.Valid {
				_m.person_credits = new(uuid.UUID)
				*_m.person_credits = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Credit.
// This includes values selected through modifiers, order, etc.
func (_m *Credit) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryItem queries the "item" edge of the Credit entity.
func (_m *Credit) QueryItem() *ItemQuery {
	return NewCreditClient(_m.config).QueryItem(_m)
}

// QueryPerson queries the "person" edge of the Credit entity.
func (_m *Credit) QueryPerson() *PersonQuery {
	return NewCreditClient(_m.config).QueryPerson(_m)
}

// Update returns a builder for updating this Credit.
// Note that you need to call Credit.Unwrap() before calling this method if this Credit
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Credit) Update() *CreditUpdateOne {
	return NewCreditClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Credit entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Credit) Unwrap() *Credit {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("store: Credit is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Credit) String() string {
	var builder strings.Builder
	builder.WriteString("Credit(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.Kind))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(_m.Role)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteByte(')')
	return builder.String()
}

// Credits is a parsable slice of Credit.
type Credits []*Credit
