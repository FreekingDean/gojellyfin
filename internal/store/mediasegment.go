// Code generated by ent, DO NOT EDIT.

package store

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/FreekingDean/gojellyfin/internal/store/item"
	"github.com/FreekingDean/gojellyfin/internal/store/mediasegment"
	"github.com/google/uuid"
)

// MediaSegment is the model entity for the MediaSegment schema.
type MediaSegment struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind mediasegment.Kind `json:"kind,omitempty"`
	// StartTicks holds the value of the "start_ticks" field.
	StartTicks int64 `json:"start_ticks,omitempty"`
	// EndTicks holds the value of the "end_ticks" field.
	EndTicks int64 `json:"end_ticks,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MediaSegmentQuery when eager-loading is set.
	Edges               MediaSegmentEdges `json:"edges"`
	item_media_segments *uuid.UUID
	selectValues        sql.SelectValues
}

// MediaSegmentEdges holds the relations/edges for other nodes in the graph.
type MediaSegmentEdges struct {
	// Item holds the value of the item edge.
	Item *Item `json:"item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ItemOrErr returns the Item value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MediaSegmentEdges) ItemOrErr() (*Item, error) {
	if e.Item != nil {
		return e.Item, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: item.Label}
	}
	return nil, &NotLoadedError{edge: "item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MediaSegment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mediasegment.FieldStartTicks, mediasegment.FieldEndTicks:
			values[i] = new(sql.NullInt64)
		case mediasegment.FieldKind:
			values[i] = new(sql.NullString)
		case mediasegment.FieldCreatedAt, mediasegment.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case mediasegment.FieldID:
			values[i] = new(uuid.UUID)
		case mediasegment.ForeignKeys[0]: // item_media_segments
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MediaSegment fields.
func (_m *MediaSegment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mediasegment.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case mediasegment.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case mediasegment.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case mediasegment.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = mediasegment.Kind(value.String)
			}
		case mediasegment.FieldStartTicks:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field start_ticks", values[i])
			} else if value.Valid {
				_m.StartTicks = value.Int64
			}
		case mediasegment.FieldEndTicks:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field end_ticks", values[i])
			} else if value.Valid {
				_m.EndTicks = value.Int64
			}
		case mediasegment.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field item_media_segments", values[i])
			} else if value.Valid {
				_m.item_media_segments = new(uuid.UUID)
				*_m.item_media_segments = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MediaSegment.
// This includes values selected through modifiers, order, etc.
func (_m *MediaSegment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryItem queries the "item" edge of the MediaSegment entity.
func (_m *MediaSegment) QueryItem() *ItemQuery {
	return NewMediaSegmentClient(_m.config).QueryItem(_m)
}

// Update returns a builder for updating this MediaSegment.
// Note that you need to call MediaSegment.Unwrap() before calling this method if this MediaSegment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MediaSegment) Update() *MediaSegmentUpdateOne {
	return NewMediaSegmentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MediaSegment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MediaSegment) Unwrap() *MediaSegment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("store: MediaSegment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MediaSegment) String() string {
	var builder strings.Builder
	builder.WriteString("MediaSegment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.Kind))
	builder.WriteString(", ")
	builder.WriteString("start_ticks=")
	builder.WriteString(fmt.Sprintf("%v", _m.StartTicks))
	builder.WriteString(", ")
	builder.WriteString("end_ticks=")
	builder.WriteString(fmt.Sprintf("%v", _m.EndTicks))
	builder.WriteByte(')')
	return builder.String()
}

// MediaSegments is a parsable slice of MediaSegment.
type MediaSegments []*MediaSegment
