



// Code generated by ent, DO NOT EDIT.



package store



	
import (
	"context"
	"errors"
	"fmt"
	"math"
	"strings"
	"sync"
	"time"
		"github.com/FreekingDean/gojellyfin/internal/store/predicate"
				"github.com/google/uuid"
			"github.com/google/uuid"
	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/schema/field"
			"database/sql/driver"
			"entgo.io/ent/dialect/sql"
			"entgo.io/ent/dialect/sql/sqlgraph"
			"entgo.io/ent/dialect/sql/sqljson"
			"entgo.io/ent/schema/field"
			 "github.com/FreekingDean/gojellyfin/internal/store/seriestimer"
			 "github.com/FreekingDean/gojellyfin/internal/store/timer"

)






// SeriesTimerQuery is the builder for querying SeriesTimer entities.
type SeriesTimerQuery struct {
	config
	ctx			*QueryContext
	order		[]seriestimer.OrderOption
	inters		[]Interceptor
	predicates 	[]predicate.SeriesTimer
		withTimers *TimerQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SeriesTimerQuery builder.
func (_q *SeriesTimerQuery) Where(ps ...predicate.SeriesTimer) *SeriesTimerQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SeriesTimerQuery) Limit(limit int) *SeriesTimerQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SeriesTimerQuery) Offset(offset int) *SeriesTimerQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SeriesTimerQuery) Unique(unique bool) *SeriesTimerQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SeriesTimerQuery) Order(o ...seriestimer.OrderOption) *SeriesTimerQuery {
	_q.order = append(_q.order, o...)
	return _q
}



	
	// QueryTimers chains the current query on the "timers" edge.
	func (_q *SeriesTimerQuery) QueryTimers() *TimerQuery {
		query := (&TimerClient{config: _q.config}).Query()
		query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
			if err := _q.prepareQuery(ctx); err != nil {
				return nil, err
			}  
	selector := _q.sqlQuery(ctx)
	if err := selector.Err(); err != nil {
		return nil, err
	}
	step := sqlgraph.NewStep(
		sqlgraph.From(seriestimer.Table, seriestimer.FieldID, selector),
		sqlgraph.To(timer.Table, timer.FieldID),
		sqlgraph.Edge(sqlgraph.O2M, false, seriestimer.TimersTable,seriestimer.TimersColumn),
	)
	fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
return fromU, nil
		}
		return query
	}


// First returns the first SeriesTimer entity from the query. 
// Returns a *NotFoundError when no SeriesTimer was found.
func (_q *SeriesTimerQuery) First(ctx context.Context) (*SeriesTimer, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ seriestimer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SeriesTimerQuery) FirstX(ctx context.Context) *SeriesTimer {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}


	// FirstID returns the first SeriesTimer ID from the query.
	// Returns a *NotFoundError when no SeriesTimer ID was found.
	func (_q *SeriesTimerQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
		var ids []uuid.UUID
		if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
			return
		}
		if len(ids) == 0 {
			err = &NotFoundError{ seriestimer.Label}
			return
		}
		return ids[0], nil
	}

	// FirstIDX is like FirstID, but panics if an error occurs.
	func (_q *SeriesTimerQuery) FirstIDX(ctx context.Context) uuid.UUID {
		id, err := _q.FirstID(ctx)
		if err != nil && !IsNotFound(err) {
			panic(err)
		}
		return id
	}


// Only returns a single SeriesTimer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SeriesTimer entity is found.
// Returns a *NotFoundError when no SeriesTimer entities are found.
func (_q *SeriesTimerQuery) Only(ctx context.Context) (*SeriesTimer, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ seriestimer.Label}
	default:
		return nil, &NotSingularError{ seriestimer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SeriesTimerQuery) OnlyX(ctx context.Context) *SeriesTimer {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}


	// OnlyID is like Only, but returns the only SeriesTimer ID in the query.
	// Returns a *NotSingularError when more than one SeriesTimer ID is found.
	// Returns a *NotFoundError when no entities are found.
	func (_q *SeriesTimerQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
		var ids []uuid.UUID
		if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
			return
		}
		switch len(ids) {
		case 1:
			id = ids[0]
		case 0:
			err = &NotFoundError{ seriestimer.Label}
		default:
			err = &NotSingularError{ seriestimer.Label}
		}
		return
	}

	// OnlyIDX is like OnlyID, but panics if an error occurs.
	func (_q *SeriesTimerQuery) OnlyIDX(ctx context.Context) uuid.UUID {
		id, err := _q.OnlyID(ctx)
		if err != nil {
			panic(err)
		}
		return id
	}


// All executes the query and returns a list of SeriesTimers.
func (_q *SeriesTimerQuery) All(ctx context.Context) ([]*SeriesTimer, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SeriesTimer, *SeriesTimerQuery]()
	return withInterceptors[[]*SeriesTimer](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SeriesTimerQuery) AllX(ctx context.Context) []*SeriesTimer {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}


	// IDs executes the query and returns a list of SeriesTimer IDs.
	func (_q *SeriesTimerQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
		if _q.ctx.Unique == nil && _q.path != nil {
			_q.Unique(true)
		}
		ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
			if err = _q.Select(seriestimer.FieldID).Scan(ctx, &ids); err != nil {
				return nil, err
			}
		return ids, nil
	}

	// IDsX is like IDs, but panics if an error occurs.
	func (_q *SeriesTimerQuery) IDsX(ctx context.Context) []uuid.UUID {
		ids, err := _q.IDs(ctx)
		if err != nil {
			panic(err)
		}
		return ids
	}


// Count returns the count of the given query.
func (_q *SeriesTimerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SeriesTimerQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SeriesTimerQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SeriesTimerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx);{
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("store: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SeriesTimerQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SeriesTimerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SeriesTimerQuery) Clone() *SeriesTimerQuery {
	if _q == nil {
		return nil
	}
	return &SeriesTimerQuery{
		config: 	_q.config,
		ctx: 		_q.ctx.Clone(),
		order: 		append([]seriestimer.OrderOption{}, _q.order...),
		inters: 	append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.SeriesTimer{}, _q.predicates...),
			withTimers: _q.withTimers.Clone(),
		// clone intermediate query.
		sql: _q.sql.Clone(),
		path: _q.path,
	}
}
	
	
	// WithTimers tells the query-builder to eager-load the nodes that are connected to
	// the "timers" edge. The optional arguments are used to configure the query builder of the edge.
	func (_q *SeriesTimerQuery) WithTimers(opts ...func(*TimerQuery)) *SeriesTimerQuery {
		query := (&TimerClient{config: _q.config}).Query()
		for _, opt := range opts {
			opt(query)
		}
		_q.withTimers = query
		return _q
	}



// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SeriesTimer.Query().
//		GroupBy(seriestimer.FieldCreatedAt).
//		Aggregate(store.Count()).
//		Scan(ctx, &v)
//
func (_q *SeriesTimerQuery) GroupBy(field string, fields ...string) *SeriesTimerGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SeriesTimerGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = seriestimer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}



// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SeriesTimer.Query().
//		Select(seriestimer.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (_q *SeriesTimerQuery) Select(fields ...string) *SeriesTimerSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SeriesTimerSelect{ SeriesTimerQuery: _q }
	sbuild.label = seriestimer.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SeriesTimerSelect configured with the given aggregations.
func (_q *SeriesTimerQuery) Aggregate(fns ...AggregateFunc) *SeriesTimerSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SeriesTimerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("store: uninitialized interceptor (forgotten import store/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !seriestimer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("store: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}


	
	




func (_q *SeriesTimerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SeriesTimer, error) {
	var (
		nodes = []*SeriesTimer{}
		_spec = _q.querySpec()
			loadedTypes = [1]bool{
					_q.withTimers != nil,
			}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SeriesTimer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SeriesTimer{config: _q.config}
		nodes = append(nodes, node)
			node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
		if query := _q.withTimers; query != nil {
			if err := _q.loadTimers(ctx, query, nodes, 
				func(n *SeriesTimer){ n.Edges.Timers = []*Timer{} },
					func(n *SeriesTimer, e *Timer){ n.Edges.Timers = append(n.Edges.Timers, e) }); err != nil {
				return nil, err
			}
		}
	return nodes, nil
}


	func (_q *SeriesTimerQuery) loadTimers(ctx context.Context, query *TimerQuery, nodes []*SeriesTimer, init func(*SeriesTimer), assign func(*SeriesTimer, *Timer)) error {
			fks := make([]driver.Value, 0, len(nodes))
			nodeids := make(map[uuid.UUID]*SeriesTimer)
			for i := range nodes {
					fks = append(fks, nodes[i].ID)
				nodeids[nodes[i].ID] = nodes[i]
					if init != nil {
						init(nodes[i])
					}
			}
				query.withFKs = true
			query.Where(predicate.Timer(func(s *sql.Selector) {
				s.Where(sql.InValues(s.C(seriestimer.TimersColumn), fks...))
			}))
			neighbors, err := query.All(ctx)
			if err != nil {
				return err
			}
			for _, n := range neighbors {
				fk := n.series_timer_timers
					if fk == nil {
						return fmt.Errorf(`foreign-key "series_timer_timers" is nil for node %v`, n.ID)
					}
				node, ok := nodeids[*fk]
				if !ok {
					return fmt.Errorf(`unexpected referenced foreign-key "series_timer_timers" returned %v for node %v`, *fk, n.ID)
				}
				assign(node, n)
			}
		return nil
	}

func (_q *SeriesTimerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
		_spec.Node.Columns = _q.ctx.Fields
		if len(_q.ctx.Fields) > 0 {
			_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
		}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SeriesTimerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(seriestimer.Table, seriestimer.Columns, sqlgraph.NewFieldSpec(seriestimer.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
			_spec.Node.Columns = append(_spec.Node.Columns, seriestimer.FieldID)
			for i := range fields {
				if fields[i] != seriestimer.FieldID {
					_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
				}
			}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}





func (_q *SeriesTimerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(seriestimer.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = seriestimer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

    

    











// SeriesTimerGroupBy is the group-by builder for SeriesTimer entities.
type SeriesTimerGroupBy struct {
	selector
	build *SeriesTimerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SeriesTimerGroupBy) Aggregate(fns ...AggregateFunc) *SeriesTimerGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SeriesTimerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeriesTimerQuery, *SeriesTimerGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}


	
	



func (_g *SeriesTimerGroupBy) sqlScan(ctx context.Context, root *SeriesTimerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds) + len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}







// SeriesTimerSelect is the builder for selecting fields of SeriesTimer entities.
type SeriesTimerSelect struct {
	*SeriesTimerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SeriesTimerSelect) Aggregate(fns ...AggregateFunc) *SeriesTimerSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SeriesTimerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeriesTimerQuery, *SeriesTimerSelect](ctx, _s.SeriesTimerQuery, _s, _s.inters, v)
}


	
	



func (_s *SeriesTimerSelect) sqlScan(ctx context.Context, root *SeriesTimerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}



    






