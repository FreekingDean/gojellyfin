// Code generated by ent, DO NOT EDIT.

package store

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/FreekingDean/gojellyfin/internal/store/playlist"
	"github.com/FreekingDean/gojellyfin/internal/store/playlistshare"
	"github.com/FreekingDean/gojellyfin/internal/store/user"
	"github.com/google/uuid"
)

// PlaylistShareCreate is the builder for creating a PlaylistShare entity.
type PlaylistShareCreate struct {
	config
	mutation *PlaylistShareMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetCreatedAt sets the "created_at" field.
func (_c *PlaylistShareCreate) SetCreatedAt(v time.Time) *PlaylistShareCreate {
	_c.mutation.SetCreatedAt(v)
	return _c
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (_c *PlaylistShareCreate) SetNillableCreatedAt(v *time.Time) *PlaylistShareCreate {
	if v != nil {
		_c.SetCreatedAt(*v)
	}
	return _c
}

// SetUpdatedAt sets the "updated_at" field.
func (_c *PlaylistShareCreate) SetUpdatedAt(v time.Time) *PlaylistShareCreate {
	_c.mutation.SetUpdatedAt(v)
	return _c
}

// SetNillableUpdatedAt sets the "updated_at" field if the given value is not nil.
func (_c *PlaylistShareCreate) SetNillableUpdatedAt(v *time.Time) *PlaylistShareCreate {
	if v != nil {
		_c.SetUpdatedAt(*v)
	}
	return _c
}

// SetCanEdit sets the "can_edit" field.
func (_c *PlaylistShareCreate) SetCanEdit(v bool) *PlaylistShareCreate {
	_c.mutation.SetCanEdit(v)
	return _c
}

// SetID sets the "id" field.
func (_c *PlaylistShareCreate) SetID(v uuid.UUID) *PlaylistShareCreate {
	_c.mutation.SetID(v)
	return _c
}

// SetPlaylistID sets the "playlist" edge to the Playlist entity by ID.
func (_c *PlaylistShareCreate) SetPlaylistID(id uuid.UUID) *PlaylistShareCreate {
	_c.mutation.SetPlaylistID(id)
	return _c
}

// SetPlaylist sets the "playlist" edge to the Playlist entity.
func (_c *PlaylistShareCreate) SetPlaylist(v *Playlist) *PlaylistShareCreate {
	return _c.SetPlaylistID(v.ID)
}

// SetUserID sets the "user" edge to the User entity by ID.
func (_c *PlaylistShareCreate) SetUserID(id uuid.UUID) *PlaylistShareCreate {
	_c.mutation.SetUserID(id)
	return _c
}

// SetUser sets the "user" edge to the User entity.
func (_c *PlaylistShareCreate) SetUser(v *User) *PlaylistShareCreate {
	return _c.SetUserID(v.ID)
}

// Mutation returns the PlaylistShareMutation object of the builder.
func (_c *PlaylistShareCreate) Mutation() *PlaylistShareMutation {
	return _c.mutation
}

// Save creates the PlaylistShare in the database.
func (_c *PlaylistShareCreate) Save(ctx context.Context) (*PlaylistShare, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *PlaylistShareCreate) SaveX(ctx context.Context) *PlaylistShare {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *PlaylistShareCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *PlaylistShareCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *PlaylistShareCreate) defaults() {
	if _, ok := _c.mutation.CreatedAt(); !ok {
		v := playlistshare.DefaultCreatedAt()
		_c.mutation.SetCreatedAt(v)
	}
	if _, ok := _c.mutation.UpdatedAt(); !ok {
		v := playlistshare.DefaultUpdatedAt()
		_c.mutation.SetUpdatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *PlaylistShareCreate) check() error {
	if _, ok := _c.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`store: missing required field "PlaylistShare.created_at"`)}
	}
	if _, ok := _c.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updated_at", err: errors.New(`store: missing required field "PlaylistShare.updated_at"`)}
	}
	if _, ok := _c.mutation.CanEdit(); !ok {
		return &ValidationError{Name: "can_edit", err: errors.New(`store: missing required field "PlaylistShare.can_edit"`)}
	}
	if len(_c.mutation.PlaylistIDs()) == 0 {
		return &ValidationError{Name: "playlist", err: errors.New(`store: missing required edge "PlaylistShare.playlist"`)}
	}
	if len(_c.mutation.UserIDs()) == 0 {
		return &ValidationError{Name: "user", err: errors.New(`store: missing required edge "PlaylistShare.user"`)}
	}
	return nil
}

func (_c *PlaylistShareCreate) sqlSave(ctx context.Context) (*PlaylistShare, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *PlaylistShareCreate) createSpec() (*PlaylistShare, *sqlgraph.CreateSpec) {
	var (
		_node = &PlaylistShare{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(playlistshare.Table, sqlgraph.NewFieldSpec(playlistshare.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = _c.conflict
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := _c.mutation.CreatedAt(); ok {
		_spec.SetField(playlistshare.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if value, ok := _c.mutation.UpdatedAt(); ok {
		_spec.SetField(playlistshare.FieldUpdatedAt, field.TypeTime, value)
		_node.UpdatedAt = value
	}
	if value, ok := _c.mutation.CanEdit(); ok {
		_spec.SetField(playlistshare.FieldCanEdit, field.TypeBool, value)
		_node.CanEdit = value
	}
	if nodes := _c.mutation.PlaylistIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   playlistshare.PlaylistTable,
			Columns: []string{playlistshare.PlaylistColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(playlist.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.playlist_shares = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   playlistshare.UserTable,
			Columns: []string{playlistshare.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.user_playlist_shares = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.PlaylistShare.Create().
//		SetCreatedAt(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.PlaylistShareUpsert) {
//			SetCreatedAt(v+v).
//		}).
//		Exec(ctx)
func (_c *PlaylistShareCreate) OnConflict(opts ...sql.ConflictOption) *PlaylistShareUpsertOne {
	_c.conflict = opts
	return &PlaylistShareUpsertOne{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *PlaylistShareCreate) OnConflictColumns(columns ...string) *PlaylistShareUpsertOne {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &PlaylistShareUpsertOne{
		create: _c,
	}
}

type (
	// PlaylistShareUpsertOne is the builder for "upsert"-ing
	//  one PlaylistShare node.
	PlaylistShareUpsertOne struct {
		create *PlaylistShareCreate
	}

	// PlaylistShareUpsert is the "OnConflict" setter.
	PlaylistShareUpsert struct {
		*sql.UpdateSet
	}
)

// SetCreatedAt sets the "created_at" field.
func (u *PlaylistShareUpsert) SetCreatedAt(v time.Time) *PlaylistShareUpsert {
	u.Set(playlistshare.FieldCreatedAt, v)
	return u
}

// UpdateCreatedAt sets the "created_at" field to the value that was provided on create.
func (u *PlaylistShareUpsert) UpdateCreatedAt() *PlaylistShareUpsert {
	u.SetExcluded(playlistshare.FieldCreatedAt)
	return u
}

// SetUpdatedAt sets the "updated_at" field.
func (u *PlaylistShareUpsert) SetUpdatedAt(v time.Time) *PlaylistShareUpsert {
	u.Set(playlistshare.FieldUpdatedAt, v)
	return u
}

// UpdateUpdatedAt sets the "updated_at" field to the value that was provided on create.
func (u *PlaylistShareUpsert) UpdateUpdatedAt() *PlaylistShareUpsert {
	u.SetExcluded(playlistshare.FieldUpdatedAt)
	return u
}

// SetCanEdit sets the "can_edit" field.
func (u *PlaylistShareUpsert) SetCanEdit(v bool) *PlaylistShareUpsert {
	u.Set(playlistshare.FieldCanEdit, v)
	return u
}

// UpdateCanEdit sets the "can_edit" field to the value that was provided on create.
func (u *PlaylistShareUpsert) UpdateCanEdit() *PlaylistShareUpsert {
	u.SetExcluded(playlistshare.FieldCanEdit)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(playlistshare.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *PlaylistShareUpsertOne) UpdateNewValues() *PlaylistShareUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(playlistshare.FieldID)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *PlaylistShareUpsertOne) Ignore() *PlaylistShareUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *PlaylistShareUpsertOne) DoNothing() *PlaylistShareUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the PlaylistShareCreate.OnConflict
// documentation for more info.
func (u *PlaylistShareUpsertOne) Update(set func(*PlaylistShareUpsert)) *PlaylistShareUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&PlaylistShareUpsert{UpdateSet: update})
	}))
	return u
}

// SetCreatedAt sets the "created_at" field.
func (u *PlaylistShareUpsertOne) SetCreatedAt(v time.Time) *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetCreatedAt(v)
	})
}

// UpdateCreatedAt sets the "created_at" field to the value that was provided on create.
func (u *PlaylistShareUpsertOne) UpdateCreatedAt() *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateCreatedAt()
	})
}

// SetUpdatedAt sets the "updated_at" field.
func (u *PlaylistShareUpsertOne) SetUpdatedAt(v time.Time) *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetUpdatedAt(v)
	})
}

// UpdateUpdatedAt sets the "updated_at" field to the value that was provided on create.
func (u *PlaylistShareUpsertOne) UpdateUpdatedAt() *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateUpdatedAt()
	})
}

// SetCanEdit sets the "can_edit" field.
func (u *PlaylistShareUpsertOne) SetCanEdit(v bool) *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetCanEdit(v)
	})
}

// UpdateCanEdit sets the "can_edit" field to the value that was provided on create.
func (u *PlaylistShareUpsertOne) UpdateCanEdit() *PlaylistShareUpsertOne {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateCanEdit()
	})
}

// Exec executes the query.
func (u *PlaylistShareUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("store: missing options for PlaylistShareCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *PlaylistShareUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *PlaylistShareUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("store: PlaylistShareUpsertOne.ID is not supported by MySQL driver. Use PlaylistShareUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *PlaylistShareUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// PlaylistShareCreateBulk is the builder for creating many PlaylistShare entities in bulk.
type PlaylistShareCreateBulk struct {
	config
	err      error
	builders []*PlaylistShareCreate
	conflict []sql.ConflictOption
}

// Save creates the PlaylistShare entities in the database.
func (_c *PlaylistShareCreateBulk) Save(ctx context.Context) ([]*PlaylistShare, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*PlaylistShare, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PlaylistShareMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = _c.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *PlaylistShareCreateBulk) SaveX(ctx context.Context) []*PlaylistShare {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *PlaylistShareCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *PlaylistShareCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.PlaylistShare.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.PlaylistShareUpsert) {
//			SetCreatedAt(v+v).
//		}).
//		Exec(ctx)
func (_c *PlaylistShareCreateBulk) OnConflict(opts ...sql.ConflictOption) *PlaylistShareUpsertBulk {
	_c.conflict = opts
	return &PlaylistShareUpsertBulk{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *PlaylistShareCreateBulk) OnConflictColumns(columns ...string) *PlaylistShareUpsertBulk {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &PlaylistShareUpsertBulk{
		create: _c,
	}
}

// PlaylistShareUpsertBulk is the builder for "upsert"-ing
// a bulk of PlaylistShare nodes.
type PlaylistShareUpsertBulk struct {
	create *PlaylistShareCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(playlistshare.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *PlaylistShareUpsertBulk) UpdateNewValues() *PlaylistShareUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(playlistshare.FieldID)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.PlaylistShare.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *PlaylistShareUpsertBulk) Ignore() *PlaylistShareUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *PlaylistShareUpsertBulk) DoNothing() *PlaylistShareUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the PlaylistShareCreateBulk.OnConflict
// documentation for more info.
func (u *PlaylistShareUpsertBulk) Update(set func(*PlaylistShareUpsert)) *PlaylistShareUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&PlaylistShareUpsert{UpdateSet: update})
	}))
	return u
}

// SetCreatedAt sets the "created_at" field.
func (u *PlaylistShareUpsertBulk) SetCreatedAt(v time.Time) *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetCreatedAt(v)
	})
}

// UpdateCreatedAt sets the "created_at" field to the value that was provided on create.
func (u *PlaylistShareUpsertBulk) UpdateCreatedAt() *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateCreatedAt()
	})
}

// SetUpdatedAt sets the "updated_at" field.
func (u *PlaylistShareUpsertBulk) SetUpdatedAt(v time.Time) *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetUpdatedAt(v)
	})
}

// UpdateUpdatedAt sets the "updated_at" field to the value that was provided on create.
func (u *PlaylistShareUpsertBulk) UpdateUpdatedAt() *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateUpdatedAt()
	})
}

// SetCanEdit sets the "can_edit" field.
func (u *PlaylistShareUpsertBulk) SetCanEdit(v bool) *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.SetCanEdit(v)
	})
}

// UpdateCanEdit sets the "can_edit" field to the value that was provided on create.
func (u *PlaylistShareUpsertBulk) UpdateCanEdit() *PlaylistShareUpsertBulk {
	return u.Update(func(s *PlaylistShareUpsert) {
		s.UpdateCanEdit()
	})
}

// Exec executes the query.
func (u *PlaylistShareUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("store: OnConflict was set for builder %d. Set it on the PlaylistShareCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("store: missing options for PlaylistShareCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *PlaylistShareUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
